# benchmarks

Reproducible synthetic benchmark comparing CP-SAT against the random and greedy baselines across instance sizes and equity modes (brief §13.2).

Subdirs: `instances` (input problems), `results` (run outputs), `notebooks` (analysis).
