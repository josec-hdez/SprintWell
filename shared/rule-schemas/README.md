# shared/rule-schemas

Source-of-truth JSON Schema for the preference-rule DSL (brief §6.1). Consumed by both the backend (Zod/class-validator) and the optimizer (Pydantic) to avoid divergence.

The v1 schema is authored in a later week-1/week-2 issue.
