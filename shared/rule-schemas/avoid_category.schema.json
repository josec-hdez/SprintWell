{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sprintwell:rule-params/AVOID_CATEGORY",
  "title": "AVOID_CATEGORY params",
  "type": "object",
  "additionalProperties": false,
  "required": ["category"],
  "properties": { "category": { "enum": ["feature", "bug", "infra", "sre", "on_call", "docs", "research"] } }
}
