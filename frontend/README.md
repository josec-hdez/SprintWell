# frontend

React + TypeScript + Vite + Tailwind + shadcn/ui single-page app. Public read-only views plus authenticated member/admin flows (brief §4.4, §10).

Per-service tooling (`package.json`, `vite.config.ts`, `tsconfig.json`, Tailwind config) is bootstrapped in later week-1 issues.
