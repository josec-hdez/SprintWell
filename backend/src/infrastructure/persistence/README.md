# infrastructure/persistence

Persistence concerns: Prisma client, mappers (Domain ↔ persistence), and concrete repository implementations.

Subdirs: `prisma` (schema + migrations), `repositories`, `mappers`.
