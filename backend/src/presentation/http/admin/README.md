# presentation/http/admin

Controllers for admin management endpoints: full CRUD of members, skills, sprints, tasks, launching planning runs, and managing any user's rules (brief §4.4). Protected by `AdminGuard`.
