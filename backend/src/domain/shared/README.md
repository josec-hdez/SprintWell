# domain/shared

Shared domain primitives reused across bounded contexts: `Id`, `Result`, `DomainEvent`, and base classes for entities/value objects/aggregates.

Pure TypeScript, no framework dependencies.
