# domain/planning

Planning bounded context: `PlanningRun` (aggregate), `PlanningStrategy` (VO), `HappinessScore` (VO), and the `PlanningRunRepository` interface (port).

Pure TypeScript, no framework dependencies.
