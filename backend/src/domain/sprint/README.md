# domain/sprint

Sprint bounded context: `Sprint` (aggregate), `Task` (entity), `TaskStatus` (VO), `Assignment` (VO), and the `SprintRepository` interface (port).

Pure TypeScript, no framework dependencies.
