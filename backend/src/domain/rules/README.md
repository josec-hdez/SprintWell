# domain/rules

Rules bounded context: `RuleSet` (aggregate), `Rule` (entity), `Weight` (VO), `RuleType` (VO), validation policies, and the `RuleSetRepository` interface (port).

Pure TypeScript, no framework dependencies. The rule DSL is defined in the brief §6.
