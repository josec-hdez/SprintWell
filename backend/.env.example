# Mirrors root .env.example DATABASE_URL — required by Prisma's default .env
# loader (Prisma reads from CWD/.env when commands run from backend/).
DATABASE_URL=postgresql://sprintwell:sprintwell@localhost:5432/sprintwell?schema=public
