# docs/methodology

AI-assisted-development log and analysis — an academic contribution of the thesis (brief §12). The per-session log (`log.md`) is activated as a separate week-1 issue; this stub fixes the directory location from the brief §14 tree.
